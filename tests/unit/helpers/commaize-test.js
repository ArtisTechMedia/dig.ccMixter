import { commaize } from '../../../helpers/commaize';
import { module, test } from 'qunit';

module('Unit | Helper | commaize');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = commaize(42);
  assert.ok(result);
});
