import { hashIt } from '../../../helpers/hash-it';
import { module, test } from 'qunit';

module('Unit | Helper | hash it');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = hashIt(42);
  assert.ok(result);
});
