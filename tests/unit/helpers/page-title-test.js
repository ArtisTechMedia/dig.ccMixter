import { pageTitle } from '../../../helpers/page-title';
import { module, test } from 'qunit';

module('Unit | Helper | page title');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = pageTitle(42);
  assert.ok(result);
});
