import { audioState } from '../../../helpers/audio-state';
import { module, test } from 'qunit';

module('Unit | Helper | audio state');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = audioState(42);
  assert.ok(result);
});
