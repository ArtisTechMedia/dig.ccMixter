import { isEmbeddable } from '../../../helpers/is-embeddable';
import { module, test } from 'qunit';

module('Unit | Helper | is embeddable');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = isEmbeddable(42);
  assert.ok(result);
});
